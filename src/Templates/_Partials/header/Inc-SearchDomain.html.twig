<div class="card mb-3 shadow-sm">
    <div class="card-body d-flex align-items-center">
        <div class="PackageListItem__icon me-3">
            <img src="#" alt="Symfony UX {{ package.name }}" class="img-fluid" />
        </div>
        <h4 class="PackageListItem__label">
            <a href="{{ package.url }}" class="text-decoration-none">{{ package.name }}</a>
        </h4>
    </div>
</div>