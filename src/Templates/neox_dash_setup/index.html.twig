{% extends 'base.html.twig' %}

{% block title %}NeoxDashSetup index{% endblock %}

{% block body %}
    <h1>NeoxDashSetup index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Logo</th>
                <th>Country</th>
                <th>Weather</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for neox_dash_setup in neox_dash_setups %}
            <tr>
                <td>{{ neox_dash_setup.id }}</td>
                <td>{{ neox_dash_setup.logo }}</td>
                <td>{{ neox_dash_setup.country }}</td>
                <td>{{ neox_dash_setup.weather }}</td>
                <td>
                    <a href="{{ path('app_neox_dash_setup_show', {'id': neox_dash_setup.id}) }}">show</a>
                    <a href="{{ path('app_neox_dash_setup_edit', {'id': neox_dash_setup.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_neox_dash_setup_new') }}">Create new</a>
{% endblock %}
